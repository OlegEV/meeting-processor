#!/usr/bin/env python3
"""
Шаблоны для генерации протоколов встреч
"""

from typing import Dict, Optional
import json
import os

class MeetingTemplates:
    """Класс для управления шаблонами протоколов встреч"""
    
    def __init__(self, templates_config_file: str = "templates_config.json"):
        self.templates_config_file = templates_config_file
        self.load_templates_config()
        
        # Встроенные шаблоны
        self.builtin_templates = {
            "standard": self._standard_template,
            "business": self._business_template,
            "project": self._project_template,
            "standup": self._standup_template,
            "interview": self._interview_template,
            "brainstorm": self._brainstorm_template,
            "review": self._review_template,
            "planning": self._planning_template,
            "technical": self._technical_template,
            "sales": self._sales_template
        }

        # Шаблоны со структурированным промптом: базовый промпт (роль, правила,
        # стиль), шаблон протокола и расшифровка хранятся отдельными блоками и
        # собираются в разные части запроса (см. get_template_parts).
        self.structured_templates = {
            "project": self._project_parts,
        }
    
    def load_templates_config(self):
        """Загружает конфигурацию шаблонов из файла"""
        try:
            if os.path.exists(self.templates_config_file):
                with open(self.templates_config_file, "r", encoding="utf-8") as f:
                    self.config = json.load(f)
            else:
                # Создаем конфигурацию по умолчанию
                self.config = {
                    "default_template": "standard",
                    "custom_templates": {},
                    "template_settings": {
                        "include_technical_info": True,
                        "include_file_datetime": True,
                        "language": "ru",
                        "max_tokens": 2000
                    }
                }
                self.save_templates_config()
        except Exception as e:
            print(f"⚠️ Ошибка загрузки конфигурации шаблонов: {e}")
            self.config = {"default_template": "standard", "custom_templates": {}}
    
    def save_templates_config(self):
        """Сохраняет конфигурацию шаблонов в файл"""
        try:
            with open(self.templates_config_file, "w", encoding="utf-8") as f:
                json.dump(self.config, f, ensure_ascii=False, indent=2)
        except Exception as e:
            print(f"⚠️ Ошибка сохранения конфигурации шаблонов: {e}")
    
    def get_template(self, template_type: str = None, transcript: str = "", file_datetime_info: dict = None) -> str:
        """Возвращает подготовленный шаблон одной строкой.

        Для шаблонов со структурированным промптом (self.structured_templates)
        блоки склеиваются в один промпт — расшифровка при этом остаётся
        отдельным блоком в теге <transcript>.
        """
        resolved_type, template = self._resolve_template(template_type, transcript)

        if template is None:
            # Структурированный шаблон — собираем плоский промпт из блоков
            return self.compose_flat_prompt(self._build_parts(resolved_type, file_datetime_info))

        template_type = resolved_type

        # Подготавливаем информацию о дате и времени
        datetime_info = ""
        datetime_display = " - указать на основе контекста или как 'не указано'"
        
        if file_datetime_info and self.config.get("template_settings", {}).get("include_file_datetime", True):
            datetime_info = f"""
Дата встречи: {file_datetime_info['date']} ({file_datetime_info['weekday_ru']})
"""
            datetime_display = f" - {file_datetime_info['datetime_full']}"
        
        # Форматируем шаблон
        try:
            formatted_template = template.format(
                datetime_info=datetime_info,
                datetime_display=datetime_display,
                transcript="{transcript}"  # Оставляем для последующей подстановки
            )
        except KeyError as e:
            print(f"⚠️ Ошибка форматирования шаблона: {e}")
            # Возвращаем шаблон как есть, если форматирование не удалось
            formatted_template = template

        return formatted_template

    def get_template_parts(self, template_type: str = None, transcript: str = "",
                           file_datetime_info: dict = None) -> Optional[Dict[str, str]]:
        """Возвращает промпт, разбитый на независимые блоки.

        Блоки:
        - base_prompt        — базовый промпт: роль, принципы, правила, стиль
                               (уходит в system-сообщение);
        - protocol_structure — шаблон протокола: только структура разделов;
        - meta               — метаданные записи (дата/время файла);
        - final_instruction  — итоговая задача, ставится последней.

        Расшифровка в блоки не входит: её подставляет вызывающий код отдельным
        блоком в теге <transcript>, чтобы данные не смешивались с инструкциями.

        Возвращает None, если у шаблона нет структурированной версии — тогда
        нужно использовать плоский get_template().
        """
        resolved_type, template = self._resolve_template(template_type, transcript, announce=False)
        if template is not None:
            return None
        return self._build_parts(resolved_type, file_datetime_info)

    def _resolve_template(self, template_type: str = None, transcript: str = "",
                          announce: bool = True):
        """Определяет итоговый тип шаблона и его плоский текст.

        Возвращает (тип, текст шаблона) либо (тип, None), если у шаблона есть
        структурированная версия. announce=False отключает логи — используется
        при предварительной проверке в get_template_parts, чтобы не дублировать
        сообщения.
        """
        if template_type is None:
            template_type = self.config.get("default_template", "standard")

        # Автоопределение типа встречи по ключевым словам
        if template_type == "auto":
            template_type = self._detect_meeting_type(transcript)
            if announce:
                print(f"🤖 Автоопределение типа встречи: {template_type}")

        # Пользовательские шаблоны имеют приоритет над встроенными
        if template_type in self.config.get("custom_templates", {}):
            if announce:
                print(f"📝 Используется пользовательский шаблон: {template_type}")
            return template_type, self.config["custom_templates"][template_type]

        if template_type in self.structured_templates:
            if announce:
                print(f"📝 Используется структурированный шаблон: {template_type}")
            return template_type, None

        if template_type in self.builtin_templates:
            if announce:
                print(f"📝 Используется встроенный шаблон: {template_type}")
            return template_type, self.builtin_templates[template_type]()

        if announce:
            print(f"⚠️ Шаблон '{template_type}' не найден, используется стандартный")
        return "standard", self.builtin_templates["standard"]()

    def _build_parts(self, template_type: str, file_datetime_info: dict = None) -> Dict[str, str]:
        """Собирает блоки структурированного шаблона и добавляет метаданные"""
        parts = dict(self.structured_templates[template_type]())
        parts["template_type"] = template_type
        parts["meta"] = self._build_meta_block(file_datetime_info)
        return parts

    def _build_meta_block(self, file_datetime_info: dict = None) -> str:
        """Формирует блок метаданных записи.

        Управляется настройкой template_settings.include_file_datetime: если она
        выключена, дата файла модели не передаётся.
        """
        if not file_datetime_info:
            return ""
        if not self.config.get("template_settings", {}).get("include_file_datetime", True):
            return ""

        return (
            "Метаданные файла записи (не часть встречи, в протокол как факты не переносить):\n"
            f"- Дата файла: {file_datetime_info.get('date', 'не указана')} "
            f"({file_datetime_info.get('weekday_ru', 'день недели не определён')})\n"
            f"- Время файла: {file_datetime_info.get('time', 'не указано')}\n"
            "Дата файла — это дата загрузки или сохранения записи, она может не совпадать "
            "с датой встречи. Если в расшифровке есть явная дата встречи, используй её. "
            "Если явной даты нет, укажи дату файла с пометкой «требует подтверждения»."
        )

    @staticmethod
    def wrap_transcript(transcript: str) -> str:
        """Оборачивает расшифровку в тег — единственный источник фактов"""
        return (
            "Материалы встречи. Это единственный источник фактов для протокола:\n\n"
            f"<transcript>\n{transcript}\n</transcript>"
        )

    @staticmethod
    def wrap_protocol_template(protocol_structure: str) -> str:
        """Оборачивает шаблон протокола в тег, отделяя его от расшифровки"""
        return (
            "Шаблон протокола. Используй именно эту структуру, включая только те "
            "разделы, для которых есть содержательная информация:\n\n"
            f"<protocol_template>\n{protocol_structure.strip()}\n</protocol_template>"
        )

    def compose_flat_prompt(self, parts: Dict[str, str]) -> str:
        """Склеивает блоки в один промпт с placeholder {transcript}.

        Используется там, где нужен именно плоский промпт (CLI-просмотр шаблона,
        обратная совместимость get_template). Порядок блоков: базовый промпт →
        метаданные → шаблон протокола → расшифровка → итоговая задача, чтобы
        инструкция стояла после данных.
        """
        blocks = [
            parts.get("base_prompt", ""),
            parts.get("meta", ""),
            self.wrap_protocol_template(parts.get("protocol_structure", "")),
            self.wrap_transcript("__TRANSCRIPT_PLACEHOLDER__"),
            parts.get("final_instruction", ""),
        ]
        text = "\n\n".join(block.strip() for block in blocks if block and block.strip())

        # Экранируем фигурные скобки: строку ещё раз обрабатывает str.format
        # в ProtocolGenerator, и случайные { } в тексте блоков его бы сломали
        text = text.replace("{", "{{").replace("}", "}}")
        return text.replace("__TRANSCRIPT_PLACEHOLDER__", "{transcript}")

    def _detect_meeting_type(self, transcript: str) -> str:
        """Автоматически определяет тип встречи по содержанию.

        Ключевые слова, минимальное число совпадений и шаблон по умолчанию
        читаются из templates_config.json. Если конфиг отсутствует или пуст,
        используется встроенный набор.
        """
        transcript_lower = transcript.lower()

        # Берём ключевые слова из конфигурации, с фоллбэком на встроенный набор
        keywords = self.config.get("meeting_type_keywords") or {
            "standup": ["стендап", "standup", "daily", "что делал", "что буду делать", "блокеры", "вчера", "сегодня"],
            "project": ["проект", "milestone", "дедлайн", "задача", "спринт", "backlog", "риск"],
            "business": ["бюджет", "roi", "прибыль", "стратегия", "клиент", "продажи", "договор"],
            "interview": ["интервью", "кандидат", "опыт работы", "навыки", "почему выбрали", "зарплата"],
            "brainstorm": ["идея", "мозговой штурм", "креатив", "предложение", "как если", "а что если"],
            "review": ["ретроспектива", "что прошло хорошо", "что улучшить", "проблемы", "feedback"],
            "technical": ["код", "архитектура", "баг", "deploy", "api", "database", "server"],
            "sales": ["продажа", "клиент", "сделка", "кво", "конверсия", "лид", "crm"]
        }

        auto_settings = self.config.get("auto_detection_settings", {})
        min_matches = auto_settings.get("min_keyword_matches", 2)
        fallback = auto_settings.get("fallback_template", "standard")

        scores = {}
        for meeting_type, words in keywords.items():
            score = sum(1 for word in words if word in transcript_lower)
            if score >= min_matches:
                scores[meeting_type] = score

        if scores:
            return max(scores, key=scores.get)

        return fallback
    
    def get_available_templates(self) -> dict:
        """Возвращает список всех доступных шаблонов"""
        builtin = list(self.builtin_templates.keys())
        custom = list(self.config.get("custom_templates", {}).keys())
        
        return {
            "builtin": builtin,
            "custom": custom,
            "all": builtin + custom
        }
    
    def add_custom_template(self, name: str, template: str):
        """Добавляет пользовательский шаблон"""
        if "custom_templates" not in self.config:
            self.config["custom_templates"] = {}
        
        self.config["custom_templates"][name] = template
        self.save_templates_config()
        print(f"✅ Добавлен пользовательский шаблон: {name}")
    
    def _standard_template(self) -> str:
        """Стандартный шаблон протокола встречи"""
        return """
Проанализируй следующий транскрипт встречи и создай структурированный протокол.
{datetime_info}
Транскрипт:
{{transcript}}

Пожалуйста, создай протокол встречи, включающий:

1. **Дата встречи**{datetime_display}
2. **Участники** - определи из транскрипта
3. **Краткое резюме встречи**
4. **Основные обсуждаемые вопросы**
5. **Принятые решения**
6. **Назначенные задачи и ответственные**
7. **Следующие шаги**
8. **Дата следующей встречи (если упоминалась)**

# Поручения
[Перечисли все назначенные на встрече задачи в формате чекбоксов:
- [ ] **Имя исполнителя** — описание задачи — ДД.ММ.ГГГГ

Если срок не указан, пиши «срок не указан» вместо даты.
Если на встрече не было назначено задач, оставь только текст: `_Действий не назначено_`.
Не выдумывай задачи, которых не было в обсуждении.]

Оформи ответ в структурированном виде с четкими заголовками.
Если дата и время не указаны в транскрипте, используй предоставленную информацию о файле.
Не вставляй заголовок первого уровня с названием документа.
"""
    
    def _business_template(self) -> str:
        """Шаблон для деловых встреч"""
        return """
Проанализируй транскрипт деловой встречи и создай официальный протокол.
{datetime_info}
Транскрипт:
{{transcript}}

Создай протокол деловой встречи в следующем формате:

# 1. Общая информация
- **Дата:** {datetime_display}
- **Участники:** [определить из транскрипта с указанием ролей]
- **Место проведения:** [если упоминается]
- **Тип встречи:** [определить по контексту]

# 2. Повестка дня
- [Основные вопросы для обсуждения]

# 3. Обсуждение
- [Детальное изложение обсужденных вопросов]

# 4. Принятые решения
- [Конкретные решения с указанием ответственных]

# 5. Поручения
[Перечисли все назначенные на встрече задачи в формате чекбоксов:
- [ ] **Имя исполнителя** — описание задачи — ДД.ММ.ГГГГ

Если срок не указан, пиши «срок не указан» вместо даты.
Если на встрече не было назначено задач, оставь только текст: `_Действий не назначено_`.
Не выдумывай задачи, которых не было в обсуждении.]

# 6. Планирование
- **Следующая встреча:** [дата/время если упоминалось]
- **Контрольные точки:** [промежуточные проверки]

# 7. Приложения
- [Упомянутые документы, ссылки, материалы]

Используй деловой стиль изложения, точные формулировки и структурированный подход.
Не вставляй заголовок первого уровня с названием документа.
"""
    
    def _project_parts(self) -> Dict[str, str]:
        """Блоки промпта для проектных встреч.

        Промпт перенесён из docs/ai_guide.md («Руководство по подготовке
        протоколов проектных встреч», раздел 2 «Готовый промпт» и раздел 16
        «Рекомендуемая структура протокола») и разделён на независимые блоки:
        базовый промпт, шаблон протокола и итоговая задача. Расшифровка
        подставляется отдельным блоком и в шаблон не вклеивается.
        """
        return {
            "base_prompt": self._project_base_prompt(),
            "protocol_structure": self._project_protocol_structure(),
            "final_instruction": self._project_final_instruction(),
        }

    def _project_template(self) -> str:
        """Плоский промпт проектной встречи (обратная совместимость).

        Собирается из тех же блоков, что и структурированная версия, — чтобы
        текст промпта не расходился между двумя путями генерации.
        """
        return self.compose_flat_prompt(self._project_parts())

    def _project_base_prompt(self) -> str:
        """Базовый промпт проектной встречи: роль, принципы, правила, стиль"""
        return """Ты — опытный проектный менеджер. Ты готовишь протокол проектной встречи в формате Markdown по расшифровке аудиозаписи.

Протокол должен быть кратким, содержательным и удобным для чтения. Не пересказывай встречу последовательно и не воспроизводи реплики участников. Обрабатывай материалы как проектный менеджер: выделяй темы, факты, решения, риски и действия.

Главный принцип: одна тема должна быть описана только один раз.

Для каждой темы:
1. Сформулируй текущее состояние или предмет обсуждения.
2. Выдели только новые и значимые тезисы.
3. Если позиции сторон различаются, кратко укажи позицию каждой стороны.
4. Зафиксируй согласованный итог либо прямо укажи, что решение не принято.
5. Укажи связанные риски, зависимости и дальнейшие действия.
6. Не повторяй эту же информацию в других разделах другими словами.

Не нужно отдельно пересказывать одну и ту же тему в разделах «Общий статус», «Статус по заказам», «Позиции сторон», «Риски» и «Принятые решения». В обзорных разделах допускается только краткое резюме в одну строку. Детальное описание должно находиться в одном основном тематическом блоке.

Правила подготовки:

1. Не придумывай факты, сроки, решения, ответственных или причинно-следственные связи.
2. Не указывай срок, если он прямо не назван или однозначно не следует из контекста. В этом случае пиши «срок не определен».
3. Не превращай предположение участника в принятое решение.
4. Различай:
   - факт;
   - позицию стороны;
   - предварительный ориентир;
   - договоренность;
   - принятое решение;
   - открытый вопрос.
5. Удаляй:
   - приветствия;
   - разговорные вставки;
   - шутки;
   - перебивания;
   - эмоциональные реакции;
   - повторные объяснения;
   - аргументы, которые не влияют на вывод;
   - историю обсуждения, если достаточно зафиксировать итог.
6. Сохраняй важные разногласия, но описывай их нейтрально и кратко.
7. Если стороны долго обсуждали одну тему, сократи обсуждение до:
   - сути разногласия;
   - позиции каждой стороны;
   - результата;
   - следующего действия.
8. Не дублируй Дальнейшие действия в тексте. В тематическом разделе можно указать только сам следующий шаг без повторения всех реквизитов.
9. Не дублируй риски. В основном тематическом разделе укажи контекст, а в таблице рисков оставь короткую формулировку, статус и митигацию.
10. Не повторяй даты в нескольких разделах без необходимости.
11. Не создавай отдельный раздел «Позиции сторон», если позиции можно компактно изложить внутри соответствующей темы.
12. Не создавай отдельный раздел ради одного тезиса.
13. Если тема не привела к решению, прямо пиши: «Решение не принято».
14. Если договоренность носит предварительный характер, используй формулировки:
    - «предварительный ориентир»;
    - «целевой срок»;
    - «рабочая гипотеза»;
    - «требует подтверждения».
15. Не используй формулировки «стороны обсудили», «было отмечено», «участники проговорили», если можно сразу написать содержательный вывод.
16. Используй короткие предложения и тезисы.
17. Каждый пункт должен содержать один смысл.
18. Не включай общеизвестный или ранее зафиксированный контекст, если он не изменился и не влияет на текущие решения.
19. При конфликте между сводкой и расшифровкой отдавай приоритет расшифровке, но учитывай, что участник мог оговориться. Спорные места помечай как требующие уточнения.
20. Перед выдачей результата проведи проверку на повторы: найди тезисы, которые встречаются более одного раза, и оставь подробное описание только в одном месте.

Требования к стилю:

- Формальный, нейтральный, деловой язык.
- Без эмоциональных оценок.
- Без разговорных оборотов.
- Без лишней канцелярщины.
- Без дословных цитат, кроме случаев, когда точная формулировка принципиальна.
- Термины и наименования систем сохраняй в едином написании.
- Названия заказов, систем, сообщений и документов не изменяй без необходимости.
- Используй Markdown.
- Итоговый протокол должен быть заметно короче расшифровки и не содержать повторного «обсасывания» одной и той же информации с разных сторон.

Работа с расшифровкой:

- Расшифровка получена автоматическим распознаванием речи и может содержать ошибки в названиях систем, продуктов, компаний, документов и в фамилиях.
- Не заменяй непонятный термин на похожий по звучанию и не «восстанавливай» название по догадке. Если название выглядит искажённым, используй тот вариант, который встречается в расшифровке чаще, и помечай его как требующий уточнения.
- Не добавляй в протокол сущности (системы, документы, номера заказов, суммы, даты), которых нет в расшифровке.
- Одно и то же название пиши единообразно во всём протоколе.
- Если фрагмент расшифровки неразборчив и смысл не восстанавливается, не строй на нём выводы.

Перед выдачей результата выполни внутреннюю проверку. Её результат не выводи — только исправь по ней протокол:

1. Можно ли удалить любой абзац без потери новой информации?
2. Повторяется ли одна тема в нескольких разделах?
3. Есть ли в протоколе тезисы, которые являются только пересказом разговора?
4. Отделены ли решения от предложений и предположений?
5. Для каждого действия понятны исполнитель, действие и срок?
6. Все ли риски действительно влияют на сроки, бюджет, объем или качество?
7. Есть ли разделы, которые можно объединить?
8. Можно ли сократить текст еще на 20% без потери управленческого смысла?
9. Каждый факт, название и срок действительно присутствует в расшифровке?

Если можно, сократи."""

    def _project_protocol_structure(self) -> str:
        """Шаблон протокола проектной встречи: только структура разделов"""
        return """**Дата встречи:**
**Статус проекта на:**
**Фаза проекта:**

## Цели встречи

## Общий статус проекта

## Статус по направлениям / заказам

## Ключевые темы встречи

Для каждой существенной темы используй структуру:

### Название темы

- Текущий статус / факты.
- Существенные уточнения.
- Позиции сторон, если они различаются.
- Согласованный итог или статус решения.
- Следующий шаг.

## Принятые решения

## Открытые вопросы и риски

| № | Вопрос / риск | Статус | Следующее действие / митигация |
|---:|---|---|---|

## Дальнейшие действия

| № | Исполнитель | Действие | Срок |
|---:|---|---|---|

## Ближайшие контрольные точки

| Дата | Контрольная точка |
|---|---|

## Следующая встреча"""

    def _project_final_instruction(self) -> str:
        """Итоговая задача: ставится последним блоком, после расшифровки"""
        return """Подготовь протокол встречи в формате Markdown по структуре из <protocol_template>, опираясь только на содержимое <transcript>.

Не вставляй заголовок первого уровня с названием документа.
Не выводи ничего, кроме самого протокола: без вступлений, пояснений, комментариев к работе и результатов внутренней проверки."""

    def _standup_template(self) -> str:
        """Шаблон для ежедневных стендапов"""
        return """
Проанализируй транскрипт ежедневного стендапа команды.
{datetime_info}
Транскрипт:
{{transcript}}

Создай краткий протокол стендапа:

# Участники
[Список участников команды]

# Что сделано вчера
**По каждому участнику:**
- [выполненные задачи]

# Планы на сегодня
**По каждому участнику:**
- [планируемые задачи]

# Блокеры и проблемы
- [проблемы, требующие решения]
- [зависимости от других команд]

# Помощь и координация
- [где нужна помощь команды]
- [координация между участниками]

# Экспресс-решения
- [быстрые решения принятые на стендапе]

# Поручения
[Перечисли все назначенные на встрече задачи в формате чекбоксов:
- [ ] **Имя исполнителя** — описание задачи — ДД.ММ.ГГГГ

Если срок не указан, пиши «срок не указан» вместо даты.
Если на встрече не было назначено задач, оставь только текст: `_Действий не назначено_`.
Не выдумывай задачи, которых не было в обсуждении.]

# Общий статус спринта
- [прогресс по целям спринта]

Будь краток, фокусируйся на действиях и блокерах.
Не вставляй заголовок первого уровня с названием документа.
"""
    
    def _interview_template(self) -> str:
        """Шаблон для интервью"""
        return """
Проанализируй транскрипт интервью и создай структурированный отчет.
{datetime_info}
Транскрипт:
{{transcript}}

Создай отчет об интервью:

# Информация
- **Дата:** {datetime_display}
- **Интервьюер:** [определить из контекста]
- **Кандидат/Интервьюируемый:** [определить]
- **Позиция/Тема:** [для чего проводилось интервью]

# Цель интервью
- [основная цель и задачи интервью]

# Основные вопросы и ответы
1. **Вопрос:** [восстановить ключевые вопросы]
   **Ответ:** [суть ответов кандидата]

# Сильные стороны
- [выявленные сильные стороны]
- [положительные моменты]

# Области для развития
- [слабые стороны или пробелы]
- [вопросы, требующие дополнительной проверки]

# Соответствие требованиям
- [насколько кандидат подходит]
- [соответствие ключевым критериям]

# Общее впечатление
- [субъективная оценка интервьюера]
- [рекомендации по дальнейшим шагам]

# Следующие шаги
- [что планируется далее]
- [дополнительные проверки или интервью]

Будь объективен, отделяй факты от впечатлений.
Не вставляй заголовок первого уровня с названием документа.
"""
    
    def _brainstorm_template(self) -> str:
        """Шаблон для мозгового штурма"""
        return """
Проанализируй транскрипт сессии мозгового штурма и структурируй идеи.
{datetime_info}
Транскрипт:
{{transcript}}

Создай отчет о мозговом штурме:

# Тема и цель
- **Дата:** {datetime_display}
- **Участники:** [креативная команда]
- **Основная проблема/вызов:** [что решали]

# Сгенерированные идеи

## Топ-идеи (наиболее обсуждаемые)
1. **Идея:** [описание]
   - **Автор:** [кто предложил]
   - **Обсуждение:** [реакция команды]

## Практичные решения
- [реализуемые в короткие сроки]

## Инновационные концепции
- [смелые и революционные идеи]

# Критерии оценки
- [какими критериями пользовались]
- [метрики для оценки идей]

# Отобранные для проработки
1. [идея + причины выбора]

# Следующие шаги
- **Исследование:** [что нужно изучить]
- **Прототипирование:** [что тестировать]
- **Ответственные:** [кто занимается проработкой]

Сохрани креативную энергию, сгруппируй идеи логично.
Не вставляй заголовок первого уровня с названием документа.
"""
    
    def _review_template(self) -> str:
        """Шаблон для ретроспектив и обзоров"""
        return """
Проанализируй транскрипт ретроспективы/обзора и создай структурированный отчет.
{datetime_info}
Транскрипт:
{{transcript}}

Создай отчет о ретроспективе:

# Информация о сессии
- **Дата:** {datetime_display}
- **Участники:** [команда]
- **Период обзора:** [что анализировали]

# Что работало хорошо
- [успехи и достижения]
- [эффективные процессы]

# Что можно улучшить
- [проблемы и сложности]
- [неэффективные процессы]

# Корневые причины
- [анализ причин проблем]
- [системные вопросы]

# Предложения по улучшению
1. **Предложение:** [конкретное улучшение]
   - **Ожидаемый эффект:**

# План действий
- **Немедленные действия:** [что делаем сразу]
- **Краткосрочные улучшения:** [1-2 недели]
- **Долгосрочные изменения:** [месяц и больше]

# Следующая ретроспектива
- **Дата:** [когда планируется]
- **Фокус:** [на что обратить внимание]

Фокусируйся на конструктивных улучшениях и действиях.
Не вставляй заголовок первого уровня с названием документа.
"""
    
    def _planning_template(self) -> str:
        """Шаблон для планирования"""
        return """
Проанализируй транскрипт встречи по планированию и создай структурированный план.
{datetime_info}
Транскрипт:
{{transcript}}

Создай протокол планирования:

# Информация
- **Дата:** {datetime_display}
- **Участники:** [команда планирования]
- **Период планирования:** [что планируем]

# Цели и задачи
- [основные цели периода]
- [ключевые результаты]

# Анализ текущей ситуации
- [с чего начинаем]
- [имеющиеся ресурсы]

# План действий
## Краткосрочные задачи (1-2 недели)
1. [задача] - [ответственный] - [дедлайн]

## Среднесрочные задачи (месяц)
1. [задача] - [ответственный] - [дедлайн]

## Долгосрочные цели (квартал)
1. [цель] - [ответственный] - [контрольные точки]

# Ресурсы и бюджет
- [необходимые ресурсы]
- [бюджетные ограничения]

# Риски и митигация
- [выявленные риски]
- [план снижения рисков]

# Контрольные точки
- [даты проверки прогресса]
- [критерии успеха]

# Поручения
[Перечисли все назначенные на встрече задачи в формате чекбоксов:
- [ ] **Имя исполнителя** — описание задачи — ДД.ММ.ГГГГ

Если срок не указан, пиши «срок не указан» вместо даты.
Если на встрече не было назначено задач, оставь только текст: `_Действий не назначено_`.
Не выдумывай задачи, которых не было в обсуждении.]

Структурируй план четко, с ясными дедлайнами и ответственными.
Не вставляй заголовок первого уровня с названием документа.
"""

    def _technical_template(self) -> str:
        """Шаблон для технических встреч"""
        return """
Проанализируй транскрипт технической встречи и создай техническую документацию.
{datetime_info}
Транскрипт:
{{transcript}}

Создай протокол технической встречи:

# Техническая информация
- **Дата:** {datetime_display}
- **Участники:** [команда разработки]
- **Системы/проекты:** [что обсуждалось]

# Архитектурные решения
- [принятые архитектурные решения]
- [изменения в дизайне системы]

# Технические проблемы
- **Выявленные баги:** [описание и приоритет]
- **Производительность:** [узкие места]
- **Безопасность:** [уязвимости и риски]

# Решения и фиксы
- [принятые технические решения]
- [план исправления проблем]

# Деплой и релизы
- [планы по деплою]
- [готовность к релизу]

# Техническая документация
- [что нужно задокументировать]
- [обновления в документации]

# Следующие шаги
- [технические задачи]
- [код-ревью и тестирование]

Используй технические термины точно, структурируй решения четко.
Не вставляй заголовок первого уровня с названием документа.
"""
    
    def _sales_template(self) -> str:
        """Шаблон для продажных встреч"""
        return """
Проанализируй транскрипт встречи по продажам и создай отчет.
{datetime_info}
Транскрипт:
{{transcript}}

Создай отчет о продажной встрече:

# Информация о встрече
- **Дата:** {datetime_display}
- **Участники:** [команда продаж]
- **Клиент/Лид:** [с кем встреча]

# Цель встречи
- [что хотели достичь]
- [этап воронки продаж]

# Презентация и предложение
- [что представляли]
- [ключевые преимущества]

# Вопросы и возражения клиента
- [основные вопросы клиента]
- [возражения и сомнения]

# Потребности клиента
- [выявленные потребности]
- [боли клиента]

# Коммерческие условия
- [обсужденная цена]
- [условия сделки]

# Прогноз сделки
- **Вероятность закрытия:** [%]
- **Сумма сделки:** [если обсуждалось]
- **Временные рамки:** [когда планируется]

# Следующие шаги
- [что нужно сделать для продвижения]
- [следующая встреча]

# Поручения
[Перечисли все назначенные на встрече задачи в формате чекбоксов:
- [ ] **Имя исполнителя** — описание задачи — ДД.ММ.ГГГГ

Если срок не указан, пиши «срок не указан» вместо даты.
Если на встрече не было назначено задач, оставь только текст: `_Действий не назначено_`.
Не выдумывай задачи, которых не было в обсуждении.]

# CRM обновления
- [что записать в CRM]
- [статус лида]

Фокусируйся на коммерческих результатах и конкретных действиях.
Не вставляй заголовок первого уровня с названием документа.
"""

def load_templates_config(config_file: str = "templates_config.json") -> dict:
    """Загружает конфигурацию шаблонов"""
    templates = MeetingTemplates(config_file)
    return templates.config

def main():
    """Функция для тестирования и управления шаблонами"""
    print("📝 УПРАВЛЕНИЕ ШАБЛОНАМИ ПРОТОКОЛОВ ВСТРЕЧ")
    print("=" * 50)
    
    templates = MeetingTemplates()
    
    print("Доступные действия:")
    print("1. Показать доступные шаблоны")
    print("2. Просмотреть шаблон")
    print("3. Создать пользовательский шаблон")
    print("4. Тест автоопределения типа встречи")
    print("5. Выход")
    
    choice = input("\nВыберите действие (1-5): ").strip()
    
    if choice == "1":
        available = templates.get_available_templates()
        print(f"\n📋 Встроенные шаблоны: {', '.join(available['builtin'])}")
        print(f"👤 Пользовательские: {', '.join(available['custom'])}")
        
    elif choice == "2":
        template_name = input("Введите название шаблона: ").strip()
        template = templates.get_template(template_name)
        print(f"\n📝 Шаблон '{template_name}':")
        print("-" * 50)
        print(template[:500] + "..." if len(template) > 500 else template)
        
    elif choice == "3":
        name = input("Название нового шаблона: ").strip()
        print("Введите шаблон (завершите вводом пустой строки):")
        lines = []
        while True:
            line = input()
            if not line:
                break
            lines.append(line)
        template_text = "\n".join(lines)
        templates.add_custom_template(name, template_text)
        
    elif choice == "4":
        test_text = input("Введите тестовый текст для определения типа: ")
        detected_type = templates._detect_meeting_type(test_text)
        print(f"🤖 Определенный тип встречи: {detected_type}")
        
    elif choice == "5":
        print("👋 До свидания!")
    else:
        print("❌ Неверный выбор")

if __name__ == "__main__":
    main()
